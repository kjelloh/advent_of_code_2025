#include "aoc25.h"
#include <vector>
#include <string>

int main() {

  if (true) {
    return solve();
  }
  else {
    // Conand generated code
    aoc25();
    std::vector<std::string> vec;
    vec.push_back("test_package");
    aoc25_print_vector(vec);
  }

}
